#include <iostream>
#include <stack>
using namespace std;

int arr[1000000]
int main(void){
	ios::sync_with_stdio(0);
	cin.tie(0);

	int n;
	stack<int> S;
	cin >> n;
	for (int i = 0; i < n; i++) cin >> arr[i];
	while (n--){
		int t;
		cin >> t;
		while ()
	}

}