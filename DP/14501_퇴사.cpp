#include <iostream>
#include <algorithm>
using namespace std;

int d[17], t[17], p[17];
int n;
int main(void){
    cin.tie(0)->sync_with_stdio(0);

    cin >> n;
    for (int i = 1; i <= n; i++) cin >> t[i] >> p[i];
    
}