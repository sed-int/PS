#include <iostream>
#include <deque>
using namespace std;

int T;
int main(void){
	ios::sync_with_stdio(0);
	cin.tie(0);
	cin >> T;
	while(T--){
		string p;
		string tmp;
		cin >> p;
		cin >> tmp;
		for (int i = 0; i < tmp.size(); i++){
			if(tmp[i] )
		}

	}
}