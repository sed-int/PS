#include <iostream>
#include <algorithm>
#include <numeric>
using namespace std;

long long H[10002];
long long D[10002];
unsigned long long kH[10002][10002];
unsigned long long Min[10002];
int n;

int main(void){
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n;
    for (int i = 0; i < n; i++){
        cin >> H[i] >> D[i];
    }
    kH[0] = H[0] + D[0]
    for (int i = 0; i < n; i++){
        kH[i] =
    }
}
